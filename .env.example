# ------------------------------------------------------------
# .env — FastAPI + SQLAlchemy + MySQL 연결 설정
# ------------------------------------------------------------
# ⚙️ 이 파일은 Python-dotenv(load_dotenv())가 읽어
#     os.getenv("DB_USER") 형태로 FastAPI 애플리케이션에서 사용됩니다.
# ⚙️ 백엔드(app/db.py)에서 DB 접속 문자열을 구성할 때 참조됩니다.
# ------------------------------------------------------------

# ------------------------------------------------------------
# [MySQL 접속 계정 설정]
# ------------------------------------------------------------
# DB_USER: MySQL 사용자 계정명
#   - moviesdb에 접속할 계정 (예: CREATE USER 'fastapiid'@'%' IDENTIFIED BY 'fastapipw';)
DB_USER=fastapiid

# DB_PASSWORD: 위 계정의 비밀번호
#   - 실제 운영에서는 .env를 git에 포함시키지 말고, 환경 변수로 직접 설정하세요.
DB_PASSWORD=fastapipw

# DB_HOST: MySQL 서버 주소
#   - 로컬 개발 시 localhost 또는 127.0.0.1
#   - 원격 서버나 Docker 컨테이너를 사용할 경우 컨테이너 이름/호스트명으로 변경
DB_HOST=127.0.0.1

# DB_PORT: MySQL 기본 포트 (기본값: 3306)
#   - 컨테이너 또는 클라우드 DB 환경에서는 다른 포트를 지정할 수도 있습니다.
DB_PORT=3306

# DB_NAME: 사용할 데이터베이스 이름
#   - CREATE DATABASE moviesdb; 로 생성한 DB 이름과 일치해야 합니다.
DB_NAME=moviesdb

# ------------------------------------------------------------
# [추천 기능 관련 환경 변수]
# ------------------------------------------------------------
# DEFAULT_LIMIT: 추천 결과의 기본 개수
#   - recommend_for_user()에서 limit 파라미터의 기본값으로 사용됩니다.
#   - 사용자가 limit를 명시하지 않으면, 이 값(기본 12)이 적용됩니다.
DEFAULT_LIMIT=12
